[project]
name = "taskpilot-sample-api"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["fastapi>=0.115,<1"]

[project.optional-dependencies]
test = ["httpx2>=2.11,<3", "pytest>=8.3,<10"]

[tool.pytest.ini_options]
testpaths = ["tests"]
